# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/s;

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "0" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    client_max_body_size 10m;

    # Proxy timeouts — aligned with Go WriteTimeout (30s) + small buffer
    proxy_connect_timeout 5s;
    proxy_send_timeout    10s;
    proxy_read_timeout    35s;

    # SPA routing: serve index.html for all non-file routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to backend services
    location /api/v1/members {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://dataaccess:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/scenarios {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://dataaccess:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/notifications {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://dataaccess:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/eligibility {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://intelligence:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/benefit {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://intelligence:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/dro {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://intelligence:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/summary-log {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://intelligence:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/crm {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://crm:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/kb {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://knowledgebase:8087;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/dq {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://dataquality:8086;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/correspondence {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://correspondence:8085;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/cases {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://casemanagement:8088;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/stages {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://casemanagement:8088;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/issues {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://issues:8092;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/errors {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://issues:8092;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/security {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://security:8093;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/migration {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://migration:8100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /ws/migration {
        proxy_pass http://migration:8100;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Tenant-ID "";
    }

    # Employer Portal services
    location /api/v1/employer {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-portal:8094;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/reporting {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-reporting:8095;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/enrollment {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-enrollment:8096;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/terminations {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-terminations:8097;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/waret {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-waret:8098;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/scp {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://employer-scp:8099;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }

    location /api/v1/health/aggregate {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://healthagg:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/preferences {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://preferences:8089;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Tenant-ID "";
    }
}
